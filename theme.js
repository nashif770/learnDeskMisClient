const theme = {
  colors: {
    // Background and Surface colors
    background: "#f8fafc", // slate-50
    surface: "#ffffff",    // white
    
    // Brand Accents
    primary: "#059669",    // emerald-600
    primaryLight: "#ecfdf5", // emerald-50
    
    // Neutral / Content colors
    textMain: "#1e293b",   // slate-800 (Rich charcoal, no pure black)
    textMuted: "#64748b",  // slate-500 (For subtext and descriptions)
    textDisabled: "#94a3b8", // slate-400 (For labels and placeholders)
    
    // Borders and Dividers
    border: "#e2e8f0",     // slate-200
    divider: "#f1f5f9",    // slate-100

    // Deep Accents (Replaces pure black)
    accentDeep: "#0f172a", // slate-900 (For high-impact cards/headers)
  },
};

export default theme;